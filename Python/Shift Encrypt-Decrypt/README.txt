crypt and decrypt random will encrypt/decrypt strings by a random shift value for each character in the string individually.
it will output the 'key' of values that will decrypt the string if encrypting or prompt for entering it if decrypting.

simple shift choose will encrypt or decrypt string as a whole by a given shift to form a simple shift code
